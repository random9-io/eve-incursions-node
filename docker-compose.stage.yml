
services:
  server:
    restart: unless-stopped
    environment:
      - NODE_ENV=production

  frontend:
    restart: unless-stopped
    ports:
      - "127.0.0.1:3011:3000"
    environment:
      - NODE_ENV=production
      - REDIS_PASSWORD

  mysql:
    restart: unless-stopped
